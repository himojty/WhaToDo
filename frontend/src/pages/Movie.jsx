import Layout from '@layouts/MainLayout/index.jsx';
import MovieSingle from "@widgets/MovieSingle/MovieSingle.jsx";

export default function MoviePage() {
    return (
        <Layout>
            <MovieSingle/>
        </Layout>
    );
}